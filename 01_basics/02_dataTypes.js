"use strict";  // Treate all JS code as newer version of JS. This means I will write new JS code.

// alert(3 + 5) // We are using nodejs and not Web Browser, hence this statement will fail to run.

let name = "Tom"
let age = 18
let isLoggedIn = false
let state;

console.log(typeof "")  // Result: string
console.log(typeof age)  // Result: number
console.log(typeof state)  // Result: undefined
console.log(typeof null)  // Result: object
console.log(typeof undefined)  // Result: undefined

